hazelcast:
  cluster-name: dev

  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: true
        member-list:
        - hazelcast1
        - hazelcast2
        - hazelcast3

  map:
    demo-map:
      backup-count: 1

  queue:
    bounded-q:
      max-size: 10